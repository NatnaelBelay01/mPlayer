import 'package:flutter/material.dart';

class ColorPallet {
  static const Color textColor = Colors.white;
	static const Color backgroundColor = Colors.black;
	static Color? greyBackgroundColor = Colors.grey[900];
}
